<?php
/**
 * Plugin Constants
 *
 * @package Apollo\{Namespace}
 */

declare(strict_types=1);

namespace Apollo\{Namespace};

// Prevent direct access.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// REST API namespace
define( 'APOLLO_{CONST}_REST_NAMESPACE', 'apollo/v1' );

// Database tables (without prefix)
// define( 'APOLLO_{CONST}_TABLE_EXAMPLE', 'apollo_{slug}_example' );

// Add plugin-specific constants below
